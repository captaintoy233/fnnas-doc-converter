version: "3.8"
services:
  docconverter:
    build: ./server
    image: docconverter:1.0.0
    container_name: doc-converter
    ports:
      - "${HOST_PORT:-8080}:8080"
    volumes:
      - docconverter_uploads:/data/uploads
    restart: unless-stopped

volumes:
  docconverter_uploads:
